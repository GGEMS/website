<footer class="site-footer">
  <div class="container footer-inner">
    <div class="footer-brand">
      <img src="/images/ggems-logo.png" alt="" />

      <div>
        <strong>GGEMS</strong>
        <p>GPU Monte Carlo for medical physics</p>
      </div>
    </div>

    <div class="footer-links">
      <a href="https://github.com/GGEMS/ggems">GitHub ↗</a>
      <a href="https://docs.ggems-project.com/">Documentation ↗</a>
      <a href="https://forum.ggems-project.com/">Forum ↗</a>
    </div>

    <p class="copyright">
      © {new Date().getFullYear()} GGEMS
    </p>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--color-border);

    background:
      linear-gradient(
        to bottom,
        rgb(255 255 255 / 1%),
        transparent
      ),
      var(--color-bg);
  }

  .footer-inner {
    min-height: 14rem;

    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 3rem;

    position: relative;
  }

  .footer-brand {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .footer-brand img {
    width: 3.25rem;
    height: 3.25rem;
  }

  .footer-brand strong {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    font-weight: 500;
    letter-spacing: 0.08em;
  }

  .footer-brand p {
    margin: 0.3rem 0 0;

    color: var(--color-text-subtle);

    font-size: 0.9rem;
  }

  .footer-links {
    display: flex;
    gap: 1.5rem;
  }

  .footer-links a {
    color: var(--color-text-muted);

    font-family: var(--font-mono);
    font-size: 0.72rem;

    text-decoration: none;

    transition: color var(--transition-fast);
  }

  .footer-links a:hover {
    color: var(--color-green-light);
  }

  .copyright {
    position: absolute;
    left: 0;
    bottom: 1.5rem;

    margin: 0;

    color: var(--color-text-subtle);

    font-family: var(--font-mono);
    font-size: 0.65rem;
  }

  @media (max-width: 720px) {
    .footer-inner {
      min-height: auto;

      grid-template-columns: 1fr;
      gap: 2.5rem;

      padding-block: 3rem 5rem;
    }

    .footer-links {
      flex-direction: column;
      gap: 0.9rem;
    }
  }
</style>
